# 粵香園財務管理系統 - 產品需求文件 (PRD)

## 1. 產品概述與視覺規範

* **產品名稱**：粵香園帳務系統
* **目標用戶**：餐廳老闆（經營者）、管理員（店長/主廚）、股東。
* **視覺與極致整潔規範 (Design System)**：
  * **主色調**：質感深紅色（如 #991B1B、#7F1D1D，簡稱 canton-red），僅用於標題、側邊欄高亮或核心按鈕，絕大面積不濫用，維持高級感。
  * **底色**：全站背景一律使用溫潤米白（#FDFBF7），卡片主體使用純白（#FFFFFF），利用微幅色差與留白替代剛硬的黑框線，達到視覺降噪。
  * **字體排版**：內文使用深灰（text-slate-700），次要說明使用淡灰（text-slate-400）。金額、百分比、日期強制使用等寬字型（font-mono）並內建千分位（如 $1,148,098），確保垂直對齊時整齊劃一。
  * **介面原則**：移動端極致防呆，按鈕加大。所有二級科目或詳細明細預設為「折疊收納」狀態，保持畫面絕對整潔。
  * **RWD 響應式規範**：
    * **電腦版（md 斷點以上）**：維持「左側固定 Sidebar (md:w-64) + 右側主工作區 (md:pl-64)」的排版。
    * **手機版（md 斷點以下）**：左側側邊欄預設完全隱藏（`-translate-x-full`），右側主工作區頂部 Header 左側補上一個「三條線漢堡選單按鈕 (☰)」。點擊時，側邊欄以抽屜式（Drawer, `z-50`）搭配半透明黑色遮罩層（Backdrop overlay）優雅滑出；點擊任何分頁或遮罩層時，抽屜需自動收回（`setIsMobileSidebarOpen(false)`）。工作區內的新增表單需自動重構為單欄直排（`grid-cols-1`）。

---

## 2. 系統功能模組詳細規格

### 模組一：身分驗證與安全登入 (Authentication)
* **登入牆機制**：全站預設受保護。未登入者一律導向登入頁，無法透過直接輸入 URL 網址繞過讀取任何資料。
* **登入方式**：
  1. **傳統密碼登入**：輸入固定的管理員帳號與密碼。
  2. **Google 帳號登入**：整合 Google OAuth 一鍵登入，自動綁定授權的管理員 Email。
* **權限控管 (RBAC)**：僅有「超級管理員（老闆）」權限者，才能進入「設定」模組。一般管理員（店長/大廚）登入後隱藏「經營支出」、「報表」與「設定」選單，防止薪資與股東分紅外洩。

### 模組二：首頁儀表板 (Dashboard) —— 預設載入首頁
* **設計定位**：作為管理員/老闆登入系統後的「預設全景鳥瞰畫面」，提供大器留白、尊榮級的經營決策維度。
* **核心元件規格**：
  1. **時間維度切換 Pills**：提供【當日】、【當週】、【當月】、【自訂區間】四種核心時間軸切換標籤。點選「自訂區間」時需展開開始與結束日期的 Date Picker。
  2. **核心 KPI 看板**：根據所選時間區間，即時動態撈取 `storage.ts` 真實數據，加總並展示餐廳的「總營收」與「總支出」。金額放大呈現（font-mono）。
  3. **支出結構分析圓餅圖**：使用優雅的純 SVG 圓餅圖/甜甜圈圖（Donut Chart），動態拆解並展示該區間內「各項支出類別（如：食材、PT薪資、房租、水電、雜支）」的金額與百分比佔比（例如：PT薪資佔 35%），幫助經營者一眼看清核心成本燒在哪裡。
  4. **前往新增快捷區**：顯眼的深紅高亮按鈕，點擊直達「每日交易」對應的新增輸入區。
  5. **待辦事項 (To-Do List)**：提供紀錄當日雜事，支援新增、勾選完成、刪除。
     * **💡【系統自動防漏提醒】**：若到每日晚上 23:30，系統偵測到「今日營收」欄位為空白，待辦事項自動置頂跳出紅字提醒：「提醒：今日尚未輸入營業收入！」

### 模組三：每日交易 (Daily Transactions)
#### 1. 營收管理
* **輸入欄位**：上午營收、下午營收、小費。
* **自動加總**：營收小計 = 上午 + 下午 + 小費。
* **💡【優化 - 財務歸屬日前一日快捷鍵】**：
  * 於「財務歸屬日」日期選擇欄位旁增設「📅 帶入前一日」小按鈕。
  * 點擊後系統自動動態計算並切換為昨天的日期（格式嚴格符合 FinancialDate），再次點擊切換回今天。此功能旨在優化現場人員於凌晨補登昨日帳目的 UX 體驗，免去手動開啟日曆之繁瑣。

#### 2. 支出管理（採用三分頁 Tab 結構，點擊切換，畫面不跳轉）
* **分頁一：現金支出**
  * **快捷項目鍵**：菜金、油條、雞、乾貨、便當盒、陳速華、吳慧芬、雜貨、其他。
  * **輸入形式**：點擊快捷鍵，彈出小巧的金額輸入框，行動端自動開啟純數字鍵盤（`input type="number"`）。
  * **💡【新增 - 常用廠商快捷備註】**：點選「菜金」後，備註欄提供下拉選單（如：阿隆蔬菜、現場補蛋），免去廚房現場手打字的麻煩。
* **分頁二：PT 薪資**
  * **快捷項目鍵**：拖地、收垃圾、林安邦、陳東海、林进賢、Dee、垃圾（廚餘）、洗碗、瓦斯。
  * **輸入形式**：點擊對應人員或項目，快速輸入當日發放的現金薪資。名稱自動同步自正職/PT名冊。
* **分頁三：支付貨款**
  * **快捷項目鍵**：檯布、惠通(一)、酒、大友(二)、惠通(二)、其他。
  * **輸入形式**：點擊快捷鍵，彈出小巧的金額輸入框，行動端自動開啟純數字鍵盤（`input type="number"`）。
  * **💡【新增 - 常用科目與供應商自動映射】**：
    * 點選快捷鍵（如「大友(二)」）後，系統需自動將該鍵名稱填入供應商（Merchant）欄位。
    * 同時，支出科目（Category）欄位將依據所選項目自動帶入對應分類（如點選「酒」則 Category 自動映射為「食材/酒水」或「其他」；點選「檯布」則映射為「雜支/營業消耗品」），並於備註欄預設帶入「支付貨款」，極大化簡化廚房與現場的手打字流程。

#### 3. 正職薪資管理
* **員工名冊功能**：老闆可在系統內「新增員工姓名並儲存」，儲存後未來輸入時會自動出現在下拉選單中。
* **兩欄式薪資填寫**：提供兩組獨立輸入框（【底薪】與【全勤/加班/補貼】）。
* **合併勾選邏輯**：旁邊設有 `[ ] 是否合併該員工薪資？` 勾選框。
  * **是**：系統自動加總（底薪+補貼），在最終報表合併為該員工的「實發總薪資」。
  * **否**：維持原樣，在詳細報表中分開兩個科目欄位呈現。
* **預留獎金比例**：可針對個別正職員工設定預留獎金比例（%）。

### 模組四：經營支出 (Operating Expenses)
#### 1. 修繕費用管理
* **常見餐廳修繕項目預設**：水電管路、冷氣清洗、廚具維修、抽油煙機保養、裝潢局部翻新、通水管。
* **均攤 12 個月邏輯**：輸入一筆修繕費後，勾選 均攤 12 個月 `[是 / 否]`。
  * **選「是」**：系統自動將總額除以 12，在未來 12 個月的報表中每月自動塞入 1/12 的成本（例如修冷氣 $120,000，每月自動計入 $10,000）。
  * **選「否」**：展開「自訂分配區」，讓老闆手動填寫接下來幾個月要如何分配這筆金額（例如：5月扣 50%、6月扣 50%）。
#### 2. 年終獎金（逐月累計儲備）
* **預設模式**：每月自動固定帶入 69,000 元。
* **智慧精算模式**：老闆可切換為「自動計算正職員工薪資」，系統會抓取正職薪資模組中的所有員工金額，自動加總後除以人數，算出「平均月薪資」，並以此平均值作為每月應動態提撥的年終獎金儲備。
#### 3. 員工紅利
* **自動連動**：自動帶出所有正職員工的姓名與基本薪資。
* **紅利試算**：自動根據報表結算出的「稅後淨利 ×10%」算出紅利總額，並提供分配金額填寫欄，讓老闆決定各員工的紅利分配。

### 模組五：報表系統 (Reports Center)
#### 1. 營運分析 (Operational Analysis)
* **💡【優化 - 自訂時間區間篩選器】**：
  * 報表中心全面補齊「自訂區間篩選器」（包含「開始日期」與「結束日期」兩個 Date Picker）。
  * 支援切換 日 / 月 / 年 / 自訂區間。當點選自訂區間後，下方所有營運分析、財務瀑布流、經營報表之數據必須即時聯動（Real-time Filter），動態撈取 localStorage 內該區間之真實數據進行計算。
* **營運組成分析**：顯示（總收入 / 總支出 / 總損益）。樹狀列出所有收入與支出科目，並自動計算「該科目佔總支出的百分比 %」與「該科目佔總營收的百分比 %」。
* **營運趨勢分析**：使用高質感純 SVG 折線圖（維護視覺一致性與輕量化，不使用 Chart.js），包含四條趨勢線：總收入、總支出、總損益、資金餘額（累計現金流）。
* **年度收支全覽**：12 個月橫向並排表格，呈現全年度的營收波動。

#### 2. 股東報表 (Shareholder Statement)
* **核心規格**：專為不參與營運的股東設計，畫面極致乾淨，隱藏所有員工個資與碎裂流水帳。支援自訂月份組合（如 1~4 月累計），修繕與年終會根據組合月份數自動加總。
* **嚴謹財務計算流（由上至下，細項預設折疊）**：
  * 營收 (Gross Revenue)
  * 支出 (- Operating Expenses，僅列出大類總額)
  * 修繕費用 (- Amortized Repair)
  * 年終獎金 (- Year-end Bonus)
  * = 稅前淨利 (Net Income Before Tax)
  * 稅後淨利：預扣 1%（此百分比輸入框可在前端自由編輯變更 % 數，系統即時動態重新計算）。
  * 員工紅利：自動扣除「稅後淨利 ×10%」。
  * 股東盈餘：稅前淨利 − 稅額 − 員工紅利。
  * 預留盈餘：提供 `[ ] %` 讓老闆自行輸入（如預留 10% 作為店面準備基金）。
  * **最終可分配盈餘**：股東盈餘 - 預留盈餘。（此為股東最終分紅大紅包，字體放大加粗）

#### 3. 餐廳經營報表 (Management Report)
* **精細化科目揭露**：
  * **薪資成本拆解**：將「正職總薪資」、「PT 總薪資（細分出拖地、洗碗等特定點工費用）」分開列出，防堵人力資源浪費。
  * **食材與物料拆解**：將「生鮮採購（菜金/雞/油條/乾貨/雜貨）」與「耗材成本（便當盒/其他）」分開。
* **💡【餐飲關鍵指標自動計算】**：報表自動算出 食材成本率 (Food Cost % = 生鮮採購 / 總營收) 與 人事成本率 (Labor Cost % = 總薪資 / 總營收)。
* **💡【經營利潤警示燈】**：當食材成本率高於 40% 或人事成本率高於 35% 時，系統該欄位文字自動變成淡紅色，提示老闆注意利潤被吃掉。
* **經營者專屬視覺化圖表**：
  * **人事與食材雙軸折線圖（雙指標對比）**：純 SVG 實現，橫軸為月份，縱軸同時對比「總營收」與「人事+食材總和」，抓出哪個月毛利下滑。
  * **點工高頻率項目圓餅圖**：純 SVG 實現，將 PT 薪資中非固定人員的雜支（如：垃圾處理、洗碗、臨時點工）單獨做成比例圖，幫老闆抓出隱形浪費。

#### 4. 工具箱：自動化通知功能 (LINE 戰報)
* **一鍵複製股東 LINE 戰報**：
  * 自動生成當日財務純文字摘要文字框（Textarea），點擊下方【一鍵複製 LINE 戰報】按鈕後，透過 `navigator.clipboard` 自動複製，方便老闆一鍵發送至股東群組。
* **電子信箱寄送與導出**：
  * 一鍵導出為 PDF / Excel。PDF 格式需完美排版、置中對齊，並完整附帶純 SVG 生成的經營趨勢折線圖與比例圓餅圖。
  * 介面提供「輸入信箱」與「立即寄送」按鈕。支援在系統內設定「定期自動排程」，每月特定日期自動將「股東報表」與「經營報表」發送至指定的股東與老闆信箱。

### 模組六：設定 (Settings)
* **帳戶設定**：管理員個人密碼更換、Google 帳號重新綁定。餐廳基本資料設定（餐廳名稱「粵香園」、報表專用 Logo 上傳、預設通知信箱）。
* **管理員設定**：只有超級管理員（老闆）可進入此列表。顯示所有在系統內有權限登入的人員名單，支援「新增管理員」填寫其帳密/Email，或對現有人員進行「一鍵停權/刪除」，保障財務隱私。

---

## 💾 數據儲存與技術落地備忘 (For Cursor Vibe Coding)
* **本地資料庫快照 (LocalStorage)**：在尚未串接後端資料庫前，系統中所有新增的交易、員工名冊、待辦事項，必須利用 localStorage 進行即時讀寫儲存，確保重新整理網頁後資料不會遺失。
* **初始化測試數據 (JavaScript Mock Data)**：請在 JavaScript 中預埋符合真實千萬營收規模的一至四月基礎數據，確保切換至「首頁」、「報表中心」時，各月份與自訂組合月份的圖表、均攤公式、預扣稅、警示燈都能立刻完美渲染。所有的 <table> 外層必須包裹一層 `<div className="w-full overflow-x-auto">`，防止大表格在手機版把網頁寬度撐爆。